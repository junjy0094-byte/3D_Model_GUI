# 3D Model Checker GUI Dependencies

# Core 3D modeling
cadquery>=2.0
jupyter-cadquery  # For native HTML export with hide/show, XY/YZ/ZX views

# Data handling
pandas

# 3D Visualization (GUI 내부 미리보기)
matplotlib>=3.5.0
numpy>=1.20.0
numpy-stl

# HTML 3D Viewer (Jupyter 없이도 가능)
plotly>=5.0.0

# GUI (tkinter는 Python 기본 포함)
